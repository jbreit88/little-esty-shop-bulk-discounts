<body>
  <h1><%= @bulk_discount.name %></h1>

  <div style="text-align: right; top:-10px;font-size:100%;">
    <%= link_to "Dashboard", merchant_dashboard_index_path(@bulk_discount.merchant_id) %>
    <%= link_to "My items", merchant_items_path(@bulk_discount.merchant_id) %>
    <%= link_to "My invoices", merchant_invoices_path(@bulk_discount.merchant_id) %>
    <%= link_to 'Discounts', merchant_bulk_discounts_path(@bulk_discount.merchant_id) %><br>
  </div>

  <h3>Quantity Threshold: <%= @bulk_discount.threshold %> </h3>
  <h3>Percentage Discount: <%= @bulk_discount.percent_discount %> </h3>
  <%= link_to 'Edit Discount', edit_merchant_bulk_discount_path(@bulk_discount.merchant_id, @bulk_discount.id) %>
</body>
